# Elixir version
elixir_version=1.16.3

# Erlang version  
erlang_version=26.2.5

# Always rebuild from scratch on every deploy?
always_rebuild=false

# Set the path the app is built into
runtime_path=/app